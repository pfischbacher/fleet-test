defaultNamespace: default

helm:
  releaseName: test
  chart: .
  valuesFiles:
    - abc.yaml

targetCustomizations:
  - name: abc-cloud
    helm:
      chart: .
      valuesFiles:
        - abc-cloud.yaml
    clusterSelector:
      matchLabels:
        env: abc
