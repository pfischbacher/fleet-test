defaultNamespace: default

helm:
  releaseName: test

targetCustomizations:
  - name: abc-cloud
    helm:
      valuesFiles:
        - abc-cloud.yaml
    clusterSelector:
      matchLabels:
        env: abc
