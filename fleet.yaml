defaultNamespace: default

helm:
  releaseName: test
  chart: .
  valuesFiles:
    - abc.yaml

targetCustomizations:
  - name: abc-cloud
    valuesFiles:
      - abc-cloud.yaml
    clusterSelector:
      matchLabels:
        env: abc
