defaultNamespace: default

helm:
  releaseName: test
  valuesFiles:
    - values.yaml

targetCustomizations:
  - name: abc-cloud
    helm:
      valuesFiles:
        - abc-cloud.yaml
    clusterSelector:
      matchLabels:
        env: abc
