defaultNamespace: default

helm:
  releaseName: test
  chart: .
  valuesFiles:
    - values.yaml

targetCustomizations:
  - name: abc-cloud
    helm:
      valuesFiles:
        - abc-cloud.yaml
    clusterSelector:
      matchLabels:
        env: abc
